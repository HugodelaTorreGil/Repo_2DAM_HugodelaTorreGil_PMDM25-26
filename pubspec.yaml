name: relacion_ejercicios_tema1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.3.2
  readmore: ^3.0.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

# Configuración del generador de iconos
flutter_icons:
  android: true
  ios: true
  image_path: "assets/img/icono_ig.png"

flutter:
  uses-material-design: true

  assets:
    - assets/img/
    - assets/img/icono_ig.png
